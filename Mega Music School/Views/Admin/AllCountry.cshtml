@model IEnumerable<Mega_Music_School.Models.Country>
@{
    ViewData["Title"] = "ListOfAddedItems";
    Layout = "~/Views/Shared/_DashboardLayout.cshtml";
}
<section class="section">
    <div class="row">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title text-center">COUNTRY/NATIONS</h5>

                    <table class="table datatable">
                        <thead>
                            <tr>
                                <th scope="col">Country Name</th>
                            </tr>
                        </thead>
                        <tbody>

                            @foreach (var country in Model)
                            {
                                <tr>
                                    <td width="30%">@country.Name</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</section>