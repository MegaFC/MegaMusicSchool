@model IEnumerable<Mega_Music_School.Models.State>
@{
    ViewData["Title"] = "ListOfAddedItems";
    Layout = "~/Views/Shared/_DashboardLayout.cshtml";
}
<section class="section">
    <div class="row">
        <div class="col-lg-12">

            <div class="card">
                <div class="card-body">
                    <h5 class="card-title text-center">STATES</h5>

                    <table class="table datatable">
                        <thead>
                            <tr>
                                <th scope="col">State Name</th>
                            </tr>
                        </thead>
                        <tbody>

                            @foreach (var stateId in Model)
                            {
                                <tr>
                                    <td width="30%">@stateId.Name </td>
                                </tr>
                            }
                        </tbody>
                    </table>

                </div>
            </div>
        </div>
    </div>
</section>