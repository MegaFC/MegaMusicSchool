@model IEnumerable<Mega_Music_School.Models.Department>
@{
    ViewData["Title"] = "ListOfAddedItems";
    Layout = "~/Views/Shared/_DashboardLayout.cshtml";
}
<section class="section">
    <div class="row">
        <div class="col-lg-12">

            <div class="card">
                <div class="card-body">
                    <h5 class="card-title text-center">DEPARTMENTS</h5>

                    <table class="table datatable">
                        <thead>
                            <tr>
                                <th scope="col">Department Name</th>
                            </tr>
                        </thead>
                        <tbody>

                            @foreach (var department in Model)
                            {
                                <tr>
                                    <td width="30%">@department.Name</td>
                                </tr>
                            }
                        </tbody>
                    </table>

                   
                </div>
            </div>
        </div>
    </div>
</section>